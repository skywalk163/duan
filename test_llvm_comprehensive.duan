打印 "=== LLVM 后端综合测试 ==="。
打印 ""。

# 测试1：基础运算
打印 "--- 测试1：基础运算 ---"。
设 a 为 10。
设 b 为 3。
打印 "a 加 b = " 加 转文本(a 加 b)。
打印 "a 减 b = " 加 转文本(a 减 b)。
打印 "a 乘 b = " 加 转文本(a 乘 b)。
打印 "a 除 b = " 加 转文本(a 除 b)。
打印 "mod(a, b) = " 加 转文本(mod(a, b))。

# 测试2：条件分支
打印 ""。
打印 "--- 测试2：条件分支 ---"。
设 x 为 真。
如果 x：
    打印 "x 为真"。
结束。

# 测试3：循环
打印 ""。
打印 "--- 测试3：循环 ---"。
设 sum 为 0。
设 i 为 0。
当 真：
    i 为 i 加 1。
    sum 为 sum 加 i。
    设 five 为 5。
    设 still_going 为 i 减 five。
    如果 still_going：
        # 继续
    否则：
        跳出。
    结束。
结束。
打印 "1+2+3+4+5 = " 加 转文本(sum)。

# 测试4：函数
打印 ""。
打印 "--- 测试4：函数 ---"。
段落 加法 接收 x y：
    返回 x 加 y。
结束。
打印 "加法(3, 4) = " 加 转文本(加法(3, 4))。

# 测试5：字符串
打印 ""。
打印 "--- 测试5：字符串 ---"。
设 s 为 "Hello"。
打印 "上写(s) = " 加 上写(s)。
打印 "小写(s) = " 加 小写(s)。
打印 "len(s) = " 加 转文本(len(s))。

# 测试6：列表
打印 ""。
打印 "--- 测试6：列表 ---"。
设 lst 为 新建列表()。
lst.追加(1)。
lst.追加(2)。
lst.追加(3)。
打印 "列表 = " 加 转文本(lst)。
打印 "长度 = " 加 转文本(len(lst))。

# 测试7：内置数学函数
打印 ""。
打印 "--- 测试7：内置数学函数 ---"。
打印 "sqrt(16) = " 加 转文本(sqrt(16))。
打印 "abs(-5) = " 加 转文本(abs(-5))。
打印 "pow(2, 3) = " 加 转文本(pow(2, 3))。
打印 "floor(3.7) = " 加 转文本(floor(3.7))。
打印 "ceil(3.2) = " 加 转文本(ceil(3.2))。

# 测试8：类型转换
打印 ""。
打印 "--- 测试8：类型转换 ---"。
打印 "to_int(3.7) = " 加 转文本(to_int(3.7))。
打印 "to_float(5) = " 加 转文本(to_float(5))。
打印 "to_string(123) = " 加 转文本(to_string(123))。

# 测试9：布尔和逻辑
打印 ""。
打印 "--- 测试9：布尔和逻辑 ---"。
设 flag 为 真。
如果 flag：
    打印 "flag 为真"。
结束。

设 flag2 为 假。
如果 not flag2：
    打印 "not flag2 为真"。
结束。

打印 ""。
打印 "=== 综合测试完成 ==="。
