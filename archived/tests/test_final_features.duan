# 段言语言特性测试 - 修复动词命名冲突

# 测试1：基本函数定义
段落 计算平方 参数 数值。
  返回 数值 乘以 数值。
结束。

# 测试2：递归函数
段落 计算阶乘 参数 数值。
  如果 数值 小于 二 那么：
    返回 一。
  结束。
  设 前值 为 数值 减 一。
  设 递归结果 为 计算阶乘 前值。
  返回 数值 乘以 递归结果。
结束。

# 测试3：条件语句
段落 判断正负 参数 数值。
  如果 数值 大于 零 那么：
    打印 正数。
    返回 一。
  结束。
  如果 数值 小于 零 那么：
    打印 负数。
    返回 负一。
  结束。
  打印 零。
  返回 零。
结束。

# 测试4：循环
段落 计算累加 参数 限制。
  设 计数器 为 零。
  设 总和 为 零。
  当 计数器 小于 限制 那么：
    设 总和 为 总和 加 计数器。
    设 计数器 为 计数器 加 一。
  结束。
  返回 总和。
结束。

# 测试5：列表操作（避免使用动词前缀）
段落 测试列表功能。
  设 数字数据 为 列表创建。
  列表追加 数字数据 一。
  列表追加 数字数据 二。
  列表追加 数字数据 三。
  列表追加 数字数据 四。
  列表追加 数字数据 五。
  
  设 数据长度 为 字符串长度 数字数据。
  打印 列表长度。
  打印 数据长度。
  
  设 索引值 为 零。
  当 索引值 小于 数据长度 那么：
    设 元素内容 为 列表获取 数字数据 索引值。
    打印 元素内容。
    设 索引值 为 索引值 加 一。
  结束。
  
  返回 数据长度。
结束。

# 测试6：字典操作
段落 测试字典功能。
  设 分数数据 为 字典创建。
  字典设置 分数数据 张三 九十五。
  字典设置 分数数据 李四 八十七。
  字典设置 分数数据 王五 九十二。
  
  设 张三分数 为 字典获取 分数数据 张三。
  打印 张三分数。
  
  返回 张三分数。
结束。

# 测试7：遍历循环
段落 测试遍历循环。
  设 累加结果 为 零。
  遍历 数字 在 数字数据 那么：
    设 累加结果 为 累加结果 加 数字。
  结束。
  打印 累加结果。
  返回 累加结果。
结束。

段落 主。
  打印 =====段言语言特性测试=====。
  
  打印 测试一函数定义。
  设 平方值 为 计算平方 五。
  打印 平方值。
  
  打印 测试二递归函数。
  设 阶乘值 为 计算阶乘 五。
  打印 阶乘值。
  
  打印 测试三条件语句。
  设 判断结果 为 判断正负 十。
  
  打印 测试四循环。
  设 累加值 为 计算累加 十。
  打印 累加值。
  
  打印 测试五列表操作。
  设 列表结果 为 测试列表功能。
  
  打印 测试六字典操作。
  设 字典结果 为 测试字典功能。
  
  打印 =====所有测试完成=====。
  返回 零。
结束。
