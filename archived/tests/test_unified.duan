# 测试统一代码生成器的段言程序

设 名字 为 "世界"。

段落 问候 接收 姓名:
    返回 "你好，" 加 姓名 加 "！"。
结束。

打印(问候(名字))。

段落 求和 接收 甲, 乙:
    返回 甲 加 乙。
结束。

设 结果 为 求和(3, 5)。
打印("3 + 5 = " 加 结果)。

类 狗:
    属性 名字。
    
    构造(名字):
        己.名字 为 名字。
    结束。
    
    段落 叫:
        打印(己.名字 加 " 汪汪叫！")。
    结束。
结束。

设 旺财 为 新建 狗("旺财")。
旺财.叫()。
