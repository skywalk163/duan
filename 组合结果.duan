# 由 段言积木桥接 v0（选块 + 内联粘合）自动合成

# 需求：对一批数字求和

# ── 积木：求和（数据）──
段落 汇总 接收 列表：
    设 合计 为 0
    设 i 为 0
    当 i 小于 长度(列表)：
        设 合计 为 合计 加 列表[i]
        设 i 为 i 加 1
    返回 合计

设 赵料 为 [1,2,3]。

# 步骤 1：对数值列表求和，返回所有元素之和
设 赵果1 为 汇总(赵料)。

打印(赵果1)。
