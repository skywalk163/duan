# 段言编程语言 - API 参考

本文档详细描述段言编程语言的语法、内置函数和标准库。

## 目录

1. [语法元素](#语法元素)
2. [数据类型](#数据类型)
3. [运算符](#运算符)
4. [控制流程](#控制流程)
5. [段落（函数）](#段落函数)
6. [类和对象](#类和对象)
7. [模块系统](#模块系统)
8. [内置函数](#内置函数)
9. [标准库](#标准库)

---

## 语法元素

### 关键字

| 关键字 | 用途 | 示例 |
|--------|------|------|
| `定义` | 变量声明 | `定义 x 等于 10。` |
| `等于` | 赋值操作 | `x 等于 20。` |
| `如果` | 条件判断 | `如果 x > 10 那么:` |
| `那么` | 条件分支开始 | `如果 x > 10 那么:` |
| `否则` | 否则分支 | `否则:` |
| `否则如果` | 多条件分支 | `否则如果 x > 5 那么:` |
| `结束` | 块结束 | `结束。` |
| `当` | 循环 | `当 i < 10:` |
| `遍历` | 遍历循环 | `遍历 x 于 列表:` |
| `返回` | 返回值 | `返回 x。` |
| `跳出` | 跳出循环 | `跳出。` |
| `跳过` | 跳过当前迭代 | `跳过。` |
| `类` | 类定义 | `《人》类:` |
| `接口` | 接口定义 | `《可打印》接口:` |
| `方法` | 方法定义 | `《说话》方法():` |
| `段` | 段落定义 | `《加》段(a, b):` |
| `新` | 实例化 | `新 人()` |
| `之` | 属性/方法访问 | `对象之属性` |
| `导出` | 导出符号 | `导出 《加》。` |
| `导入` | 导入符号 | `导入 《加》。` |
| `从` | 从模块导入 | `从 math 导入 《加》。` |
| `真` | 布尔真值 | `定义 开关 等于 真。` |
| `假` | 布尔假值 | `定义 开关 等于 假。` |
| `空` | 空值 | `定义 x 等于 空。` |
| `打印` | 输出到控制台 | `打印("你好")。` |
| `输出` | 输出到控制台 | `输出("你好")。` |
| `尝试` | 异常处理 | `尝试:` |
| `捕获` | 捕获异常 | `捕获 错误:` |
| `抛出` | 抛出异常 | `抛出 错误。` |

### 标点符号

| 符号 | 用途 | 示例 |
|------|------|------|
| `《》` | 段落名/方法名 | `《加》段(a, b):` |
| `：` 或 `:` | 块开始 | `如果 x > 10 那么:` |
| `。` | 语句结束 | `定义 x 等于 10。` |
| `，` 或 `,` | 参数分隔 | `《加》(1, 2)` |
| `[]` | 列表 | `[1, 2, 3]` |
| `{}` 或 `_典()` | 字典 | `_典("名": "值")` |

---

## 数据类型

### 基本类型

| 类型 | 描述 | 示例 |
|------|------|------|
| 数 | 整数或浮点数 | `42`, `3.14` |
| 串 | 字符串 | `"你好"` |
| 布尔 | 布尔值 | `真`, `假` |
| 空 | 空值 | `空` |
| 列表 | 有序集合 | `[1, 2, 3]` |
| 字典 | 键值对集合 | `_典("名": "值")` |

### 类型检查

```段言
定义 x 等于 42。
定义 类型 等于 type(x)。  # 返回 "数"
```

---

## 运算符

### 算术运算符

| 运算符 | 中文形式 | 示例 |
|--------|----------|------|
| 加法 | `加` | `10 加 5` |
| 减法 | `减` | `10 减 5` |
| 乘法 | `乘` | `10 乘 5` |
| 除法 | `除` | `10 除 5` |
| 取模 | `模` | `10 模 3` |
| 幂运算 | `幂` | `2 幂 10` |

### 比较运算符

| 运算符 | 中文形式 | 示例 |
|--------|----------|------|
| 等于 | `等于` | `x 等于 10` |
| 不等于 | `不等于` | `x 不等于 10` |
| 大于 | `大于` | `x 大于 10` |
| 小于 | `小于` | `x 小于 10` |
| 大于等于 | `大于等于` | `x 大于等于 10` |
| 小于等于 | `小于等于` | `x 小于等于 10` |

### 逻辑运算符

| 运算符 | 中文形式 | 示例 |
|--------|----------|------|
| 且 | `且` | `真 且 假` |
| 或 | `或` | `真 或 假` |
| 非 | `非` | `非 真` |

---

## 控制流程

### 条件语句

```段言
如果 条件 那么:
    # 条件为真时执行
否则如果 其他条件 那么:
    # 其他条件为真时执行
否则:
    # 所有条件都不满足时执行
结束。
```

### 循环语句

```段言
# 当循环
当 条件:
    # 循环体
    跳出。  # 可选：跳出循环
    跳过。  # 可选：跳过当前迭代
结束。

# 遍历循环
遍历 元素 于 列表:
    打印(元素)。
结束。
```

---

## 段落（函数）

### 定义段落

```段言
《段落名》段(参数1, 参数2):
    # 段落体
    返回 结果。
结束。
```

### 调用段落

```段言
定义 结果 等于 《段落名》(参数1, 参数2)。
```

### 递归调用

```段言
《阶乘》段(n):
    如果 n 小于等于 1 那么:
        返回 1。
    结束。
    返回 n 乘 《阶乘》(n 减 1)。
结束。
```

---

## 类和对象

### 定义类

```段言
《类名》类:
    定义 属性1 等于 默认值1。
    定义 属性2 等于 默认值2。
    
    《初始化》方法(参数1, 参数2):
        属性1 等于 参数1。
        属性2 等于 参数2。
    结束。
    
    《方法名》方法():
        # 方法体
    结束。
结束。
```

### 实例化对象

```段言
定义 对象 等于 新 类名(参数1, 参数2)。
```

### 访问属性和方法

```段言
# 访问属性
定义 值 等于 对象之属性1。

# 调用方法
对象之方法名()。
```

### 继承

```段言
《子类》类 继承 父类:
    # 子类定义
结束。
```

---

## 模块系统

### 导出符号

```段言
导出 《段落名》。
导出 类名。
```

### 导入符号

```段言
# 从模块导入特定符号
从 模块名 导入 《段落名》。
从 模块名 导入 类名。

# 导入多个符号
从 模块名 导入 《段落1》, 《段落2》。
```

### 模块文件结构

```
modules/
├── math_utils.duan    # 数学工具模块
├── string_utils.duan  # 字符串工具模块
└── data_models.duan   # 数据模型模块
```

---

## 内置函数

### 数学函数

| 函数 | 描述 | 示例 |
|------|------|------|
| `abs(x)` | 绝对值 | `abs(-42)` → 42 |
| `sqrt(x)` | 平方根 | `sqrt(16)` → 4.0 |
| `sin(x)` | 正弦 | `sin(0)` → 0 |
| `cos(x)` | 余弦 | `cos(0)` → 1 |
| `tan(x)` | 正切 | `tan(0)` → 0 |
| `log(x)` | 自然对数 | `log(10)` |
| `exp(x)` | e的幂 | `exp(1)` |
| `floor(x)` | 向下取整 | `floor(3.7)` → 3 |
| `ceil(x)` | 向上取整 | `ceil(3.2)` → 4 |
| `阶乘(n)` | 阶乘 | `阶乘(5)` → 120 |
| `平均数(列表)` | 平均值 | `平均数([1,2,3,4,5])` → 3.0 |
| `中位数(列表)` | 中位数 | `中位数([1,2,3,4,5])` → 3 |
| `求和(列表)` | 求和 | `求和([1,2,3])` → 6 |
| `π` | 圆周率常数 | `π` → 3.14159... |
| `e` | 自然常数 | `e` → 2.71828... |
| `随机整数(最小, 最大)` | 随机整数 | `随机整数(1, 100)` → 42 |
| `随机浮点()` | 随机浮点数 | `随机浮点()` → 0.5 |

### 字符串函数

| 函数 | 描述 | 示例 |
|------|------|------|
| `len(串)` | 长度 | `len("你好")` → 2 |
| `upper(串)` | 大写 | `upper("hello")` → "HELLO" |
| `lower(串)` | 小写 | `lower("HELLO")` → "hello" |
| `strcmp(串1, 串2)` | 比较 | `strcmp("a", "b")` |
| `substring(串, 开始, 结束)` | 子串 | `substring("hello", 0, 2)` |
| `replace(串, 旧, 新)` | 替换 | `replace("hello", "l", "L")` |
| `split(串, 分隔符)` | 分割 | `split("a,b,c", ",")` |
| `indexOf(串, 子串)` | 查找位置 | `indexOf("hello", "l")` |
| `contains(串, 子串)` | 包含检查 | `contains("hello", "ell")` |

### 列表函数

| 函数 | 描述 | 示例 |
|------|------|------|
| `listLen(列表)` | 长度 | `listLen([1,2,3])` → 3 |
| `listAppend(列表, 元素)` | 添加元素 | `listAppend(lst, 4)` |
| `listInsert(列表, 位置, 元素)` | 插入 | `listInsert(lst, 0, "x")` |
| `listRemove(列表, 位置)` | 移除 | `listRemove(lst, 0)` |
| `listPop(列表)` | 弹出末尾 | `listPop(lst)` |
| `listSort(列表)` | 排序 | `listSort(lst)` |

### 类型转换函数

| 函数 | 描述 | 示例 |
|------|------|------|
| `_串化(x)` | 转字符串 | `_串化(42)` → "42" |
| `_数化(串)` | 数字 | `_数化("42")` → 42 |
| `_布尔化(x)` | 转布尔 | `_布尔化(1)` → 真 |
| `type(x)` | 获取类型 | `type(42)` → "数" |

### 其他函数

| 函数 | 描述 | 示例 |
|------|------|------|
| `打印(x)` | 输出到控制台 | `打印("你好")` |
| `输出(x)` | 输出到控制台 | `输出("你好")` |
| `now()` | 当前时间戳 | `now()` |
| `sleep(秒)` | 等待 | `sleep(1)` |
| `_文件存在(路径)` | 文件检查 | `_文件存在("test.txt")` |
| `range(开始, 结束)` | 生成范围 | `range(0, 10)` |
| `当前时间(格式)` | 获取当前时间 | `当前时间("%Y-%m-%d")` |
| `当前日期()` | 获取当前日期 | `当前日期()` |
| `当前时间戳()` | 获取 Unix 时间戳 | `当前时间戳()` |
| `解析时间(字符串, 格式)` | 解析时间字符串 | `解析时间("2024-01-01", "%Y-%m-%d")` |
| `解析JSON/parseJSON(字符串)` | 解析 JSON | `parseJSON('{"a":1}')` |
| `序列化JSON(值, 缩进)` | 序列化 JSON | `序列化JSON({"a":1}, 2)` |
| `MD5(文本)` | MD5 哈希 | `MD5("hello")` |
| `SHA256(文本)` | SHA256 哈希 | `SHA256("hello")` |
| `Base64编码(文本)` | Base64 编码 | `Base64编码("段言")` |
| `Base64解码(文本)` | Base64 解码 | `Base64解码("5q616KiA")` |
| `匹配(模式, 字符串)` | 正则匹配 | `匹配("\\d+", "123")` |
| `搜索(模式, 字符串)` | 正则搜索 | `搜索("world", "hello world")` |
| `替换(模式, 替换为, 字符串)` | 正则替换 | `替换("\\s+", "_", "a b")` |

---

## 标准库

### 日期时间模块

```段言
设 时间 为 当前时间("%Y-%m-%d %H:%M:%S")。
设 日期 为 当前日期()。
设 时间戳 为 当前时间戳()。
设 格式化 为 格式化时间(时间戳, "%Y/%m/%d")。
设 天数 为 日期差("2024-01-01", "2024-12-31")。
```

### JSON 模块

```段言
设 数据 为 解析JSON('{"name": "段言", "version": 1}')。
设 json串 为 序列化JSON(数据, 2)。
```

### 哈希与编码模块

```段言
设 md5值 为 MD5("hello")。
设 sha值 为 SHA256("hello")。
设 编码 为 Base64编码("段言")。
设 解码 为 Base64解码("5q616KiA")。
```

---

## 命令行工具

### duan_cli.py

```bash
# 运行段言脚本
python duan_cli.py run script.duan

# 编译为可执行文件
python duan_cli.py compile script.duan -o output.exe

# 解析并显示AST
python duan_cli.py parse script.duan
```

### duan_repl.py

```bash
# 启动交互式REPL
python duan_repl.py
```

---

## 错误处理

### 异常捕获

```段言
尝试:
    # 可能出错的代码
捕获 错误:
    打印("错误: " + 错误)。
结束。
```

### 抛出异常

```段言
抛出 "参数错误"。
```

---

## 版本信息

- 版本: 1.0.0
- 作者: 段言开发团队
- 许可: MIT