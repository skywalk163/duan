# 段言标准库 - 字符串工具模块（纯段言实现）
#
# 用法：
#   从 字符串工具 导入 反转, 重复。

# 反转字符串
段落 反转。
  参数 原文。
  变量 结果 = ""。
  变量 长度 = 长度(原文)。
  变量 索引 = 长度 - 1。
  当 索引 >= 0：
    结果 = 结果 + 取字符(原文, 索引)。
    索引 = 索引 - 1。
  返回 结果。
结束。

# 重复字符串
段落 重复。
  参数 原文 次数。
  变量 结果 = ""。
  变量 计数 = 0。
  当 计数 < 次数：
    结果 = 结果 + 原文。
    计数 = 计数 + 1。
  返回 结果。
结束。

# 判断包含
段落 包含。
  参数 原文 子串。
  变量 结果 = 查找(原文, 子串)。
  如果 结果 >= 0：
    返回 真。
  返回 假。
结束。

# 判断前缀
段落 开头是。
  参数 原文 前缀。
  变量 前缀长度 = 长度(前缀)。
  如果 长度(原文) < 前缀长度：
    返回 假。
  变量 索引 = 0。
  当 索引 < 前缀长度：
    如果 取字符(原文, 索引) != 取字符(前缀, 索引)：
      返回 假。
    索引 = 索引 + 1。
  返回 真。
结束。

# 判断后缀
段落 结尾是。
  参数 原文 后缀。
  变量 原文长度 = 长度(原文)。
  变量 后缀长度 = 长度(后缀)。
  如果 原文长度 < 后缀长度：
    返回 假。
  变量 索引 = 0。
  当 索引 < 后缀长度：
    如果 取字符(原文, 原文长度 - 后缀长度 + 索引) != 取字符(后缀, 索引)：
      返回 假。
    索引 = 索引 + 1。
  返回 真。
结束。

# 统计子串出现次数
段落 计数。
  参数 原文 子串。
  变量 次数 = 0。
  变量 索引 = 0。
  变量 子串长度 = 长度(子串)。
  如果 子串长度 == 0：
    返回 0。
  变量 原文长度 = 长度(原文)。
  当 索引 <= 原文长度 - 子串长度：
    变量 匹配 = 真。
    变量 偏移 = 0。
    当 偏移 < 子串长度：
      如果 取字符(原文, 索引 + 偏移) != 取字符(子串, 偏移)：
        匹配 = 假。
      偏移 = 偏移 + 1。
    如果 匹配：
      次数 = 次数 + 1。
      索引 = 索引 + 子串长度。
    否则：
      索引 = 索引 + 1。
  返回 次数。
结束。

# 去除首尾空格
段落 去空格。
  参数 原文。
  变量 开始 = 0。
  变量 结束 = 长度(原文) - 1。
  当 开始 <= 结束：
    如果 取字符(原文, 开始) == " "：
      开始 = 开始 + 1。
    否则：
      跳出。
  当 结束 >= 开始：
    如果 取字符(原文, 结束) == " "：
      结束 = 结束 - 1。
    否则：
      跳出。
  返回 截取(原文, 开始, 结束 + 1)。
结束。

导出 反转 重复 包含 开头是 结尾是。
导出 计数 去空格。
