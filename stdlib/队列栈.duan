# 段言标准库 v3.2 - 队列栈模块
#
# 用法：
#   从 队列栈 导入 创建栈, 入栈, 出栈。

# 栈操作
段落 创建栈：
    返回 []

段落 入栈 接收 栈, 值：
    栈.追加(值)

段落 出栈 接收 栈：
    如果 栈为空：
        报错 "栈为空"
    返回 栈.弹出()

段落 查看栈顶 接收 栈：
    如果 栈为空：
        报错 "栈为空"
    返回 栈[-1]

段落 栈是否为空 接收 栈：
    返回 长度(栈) 等于 0

段落 栈大小 接收 栈：
    返回 长度(栈)

# 队列操作
段落 创建队列：
    返回 双端队列()

段落 入队 接收 队列, 值：
    队列.追加(值)

段落 出队 接收 队列：
    如果 队列为空：
        报错 "队列为空"
    返回 队列.左弹出()

段落 查看队首 接收 队列：
    如果 队列为空：
        报错 "队列为空"
    返回 队列[0]

段落 队列是否为空 接收 队列：
    返回 长度(队列) 等于 0

段落 队列大小 接收 队列：
    返回 长度(队列)

# 双端队列操作
段落 创建双端队列：
    返回 双端队列()

段落 左入 接收 双端队列, 值：
    双端队列.左追加(值)

段落 右入 接收 双端队列, 值：
    双端队列.追加(值)

段落 左出 接收 双端队列：
    如果 双端队列为空：
        报错 "双端队列为空"
    返回 双端队列.左弹出()

段落 右出 接收 双端队列：
    如果 双端队列为空：
        报错 "双端队列为空"
    返回 双端队列.弹出()

段落 查看左端 接收 双端队列：
    如果 双端队列为空：
        报错 "双端队列为空"
    返回 双端队列[0]

段落 查看右端 接收 双端队列：
    如果 双端队列为空：
        报错 "双端队列为空"
    返回 双端队列[-1]

段落 双端队列是否为空 接收 双端队列：
    返回 长度(双端队列) 等于 0

段落 双端队列大小 接收 双端队列：
    返回 长度(双端队列)

导出 创建栈 入栈 出栈 查看栈顶 栈是否为空 栈大小。
导出 创建队列 入队 出队 查看队首 队列是否为空 队列大小。
导出 创建双端队列 左入 右入 左出 右出。
导出 查看左端 查看右端 双端队列是否为空 双端队列大小。
