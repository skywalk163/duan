# 段言标准库 - 中文 NLP 模块
#
# 提供综合的中文自然语言处理功能：
# - 分词（jieba 精确/全/搜索引擎模式，词性标注，关键词提取）
# - 拼音转换（pypinyin 无音调/带声调/声调数字/首字母/姓氏）
# - 简繁转换（简体繁体互转，台湾/香港变体）
# - 文本统计（字符统计、词频、句子/段落、可读性、语言检测）
# - 数字与金额转换（中文数字、大写金额、百分比）
# - 文本处理工具（提取、相似度、敏感词过滤）
#
# 依赖（可选）:
#   pip install jieba
#   pip install pypinyin
#   pip install opencc-python-reimplemented
#
# 用法：
#   从 中文NLP 导入 分词精确模式, 转拼音, 金额转大写, 敏感词过滤器。

# =============================================================================
# 1. 分词 (Tokenization)
# =============================================================================

# 精确模式分词
段落 分词精确模式(文本):
    从 中文NLP 加载 分词精确模式(文本)。

# 全模式分词
段落 分词全模式(文本):
    从 中文NLP 加载 分词全模式(文本)。

# 搜索引擎模式分词
段落 分词搜索引擎模式(文本):
    从 中文NLP 加载 分词搜索引擎模式(文本)。

# 添加自定义词典
段落 添加自定义词典(词典路径):
    从 中文NLP 加载 添加自定义词典(词典路径)。

# 添加自定义词语
段落 添加自定义词语(词, 词频=0, 词性=无):
    从 中文NLP 加载 添加自定义词语(词, 词频, 词性)。

# 删除自定义词语
段落 删除自定义词语(词):
    从 中文NLP 加载 删除自定义词语(词)。

# 词性标注
段落 词性标注(文本):
    从 中文NLP 加载 词性标注(文本)。

# 提取关键词 (TF-IDF)
段落 提取关键词TFIDF(文本, 数量=10):
    从 中文NLP 加载 提取关键词TFIDF(文本, 数量)。

# 提取关键词 (TextRank)
段落 提取关键词TextRank(文本, 数量=10):
    从 中文NLP 加载 提取关键词TextRank(文本, 数量)。

# 分词带位置
段落 分词带位置(文本):
    从 中文NLP 加载 分词带位置(文本)。

# =============================================================================
# 2. 拼音转换 (Pinyin)
# =============================================================================

# 转拼音（无音调）
段落 转拼音(文本, 分隔符=" ", 首字母=假):
    从 中文NLP 加载 转拼音(文本, 分隔符, 首字母)。

# 转拼音（带声调）
段落 转拼音带声调(文本, 分隔符=" "):
    从 中文NLP 加载 转拼音带声调(文本, 分隔符)。

# 转拼音（声调数字）
段落 转拼音声调数字(文本, 分隔符=" "):
    从 中文NLP 加载 转拼音声调数字(文本, 分隔符)。

# 转拼音首字母
段落 转拼音首字母(文本):
    从 中文NLP 加载 转拼音首字母(文本)。

# 转拼音列表
段落 转拼音列表(文本, 带声调=假):
    从 中文NLP 加载 转拼音列表(文本, 带声调)。

# 转拼音（姓氏优先）
段落 转拼音姓氏(文本, 分隔符=" "):
    从 中文NLP 加载 转拼音姓氏(文本, 分隔符)。

# =============================================================================
# 3. 简繁转换 (Simplified/Traditional)
# =============================================================================

# 简体转繁体
段落 简转繁(文本):
    从 中文NLP 加载 简转繁(文本)。

# 繁体转简体
段落 繁转简(文本):
    从 中文NLP 加载 繁转简(文本)。

# 简体转台湾繁体
段落 简转繁台湾(文本):
    从 中文NLP 加载 简转繁台湾(文本)。

# 简体转香港繁体
段落 简转繁香港(文本):
    从 中文NLP 加载 简转繁香港(文本)。

# =============================================================================
# 4. 文本统计 (Text Statistics)
# =============================================================================

# 统计字符
段落 统计字符(文本):
    从 中文NLP 加载 统计字符(文本)。

# 统计词频
段落 统计词频(文本, 数量=10, 过滤停用词=假):
    从 中文NLP 加载 统计词频(文本, 数量, 过滤停用词)。

# 统计句子数
段落 统计句子数(文本):
    从 中文NLP 加载 统计句子数(文本)。

# 统计段落数
段落 统计段落数(文本):
    从 中文NLP 加载 统计段落数(文本)。

# 可读性评分
段落 可读性评分(文本):
    从 中文NLP 加载 可读性评分(文本)。

# 语言检测
段落 语言检测(文本):
    从 中文NLP 加载 语言检测(文本)。

# =============================================================================
# 5. 数字与金额转换 (Number/Currency)
# =============================================================================

# 数字转中文
段落 数字转中文(数值, 大写=假):
    从 中文NLP 加载 数字转中文(数值, 大写)。

# 中文转数字
段落 中文转数字(文本):
    从 中文NLP 加载 中文转数字(文本)。

# 金额转大写
段落 金额转大写(金额):
    从 中文NLP 加载 金额转大写(金额)。

# 百分比转中文
段落 百分比转中文(数值, 小数位数=2):
    从 中文NLP 加载 百分比转中文(数值, 小数位数)。

# 中文转百分比
段落 中文转百分比(文本):
    从 中文NLP 加载 中文转百分比(文本)。

# =============================================================================
# 6. 文本处理工具 (Text Utilities)
# =============================================================================

# 去除空白
段落 去除空白(文本):
    从 中文NLP 加载 去除空白(文本)。

# 去除标点
段落 去除标点(文本):
    从 中文NLP 加载 去除标点(文本)。

# 提取汉字
段落 提取汉字(文本):
    从 中文NLP 加载 提取汉字(文本)。

# 提取英文
段落 提取英文(文本):
    从 中文NLP 加载 提取英文(文本)。

# 提取数字
段落 提取数字(文本):
    从 中文NLP 加载 提取数字(文本)。

# 判断中英混合
段落 判断中英混合(文本):
    从 中文NLP 加载 判断中英混合(文本)。

# 文本相似度 (Jaccard)
段落 文本相似度Jaccard(文本1, 文本2):
    从 中文NLP 加载 文本相似度Jaccard(文本1, 文本2)。

# 文本相似度 (余弦)
段落 文本相似度余弦(文本1, 文本2):
    从 中文NLP 加载 文本相似度余弦(文本1, 文本2)。

# 敏感词过滤器（类）
段落 敏感词过滤器():
    从 中文NLP 加载 敏感词过滤器。

# =============================================================================
# 导出
# =============================================================================

导出 分词精确模式 分词全模式 分词搜索引擎模式。
导出 添加自定义词典 添加自定义词语 删除自定义词语。
导出 词性标注 提取关键词TFIDF 提取关键词TextRank 分词带位置。

导出 转拼音 转拼音带声调 转拼音声调数字。
导出 转拼音首字母 转拼音列表 转拼音姓氏。

导出 简转繁 繁转简 简转繁台湾 简转繁香港。

导出 统计字符 统计词频 统计句子数 统计段落数。
导出 可读性评分 语言检测。

导出 数字转中文 中文转数字 金额转大写。
导出 百分比转中文 中文转百分比。

导出 去除空白 去除标点 提取汉字 提取英文 提取数字。
导出 判断中英混合。
导出 文本相似度Jaccard 文本相似度余弦。
导出 敏感词过滤器。