# 段言标准库 - 数学模块
#
# 用法：
#   从《数学》导入《平方根》，《幂》。

导出 绝对值 最大值 最小值。
导出 幂 平方根。
导出 正弦 余弦 正切。
导出 弧度转角度 角度转弧度。
导出 向上取整 向下取整 四舍五入。
导出 随机整数 随机浮点 随机选择。
导出 圆周率 自然常数。
导出 阶乘 对数 自然对数。
导出 平均数 中位数 众数。
导出 标准差 样本标准差 方差 样本方差。
导出 范围 求和 累积和。
导出 线性回归。