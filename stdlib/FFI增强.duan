# 段言 C FFI 增强模块
# 提供简化 C 库加载和调用的语法

# =============================================================================
# 简化 C 库加载
# =============================================================================
# 语法：加载C库("库名")
# 自动检测平台扩展名（.so/.dylib/.dll）
# 支持库名缩写：c、m、pthread、dl

段落 加载C库 接收 库名：文本：
    从 FFI增强 加载 加载C库(库名)
    。

# =============================================================================
# 简化 C 函数调用
# =============================================================================
# 语法：调用C函数(lib, "函数名", 参数...)
# 无需手动声明函数签名，自动传递参数

段落 调用C函数 接收 库, 函数名：文本, 参数列表：
    从 FFI增强 加载 调用C函数(库, 函数名, *参数列表)
    。

# =============================================================================
# 带类型声明的函数调用
# =============================================================================
# 语法：声明并调用C函数(lib, "函数名", [参数类型...], 返回类型, 参数...)
# 适合需要精确控制类型的场景

段落 声明并调用C函数 接收 库, 函数名：文本, 参数类型列表, 返回类型, 参数列表：
    从 FFI增强 加载 声明并调用C函数(库, 函数名, 参数类型列表, 返回类型, *参数列表)
    。

# =============================================================================
# 预设绑定
# =============================================================================
# 预设绑定提供了常用 C 库函数的类型签名，开箱即用。
#
# 预设绑定.libc    - 标准 C 库（printf, malloc, strlen, strcmp, memset 等）
# 预设绑定.libm    - 数学库（sin, cos, sqrt, pow, log, exp 等）
# 预设绑定.libpthread - POSIX 线程库
# 预设绑定.libdl   - 动态加载库
#
# 示例：
#   设 绑定 为 从 FFI增强 加载 预设绑定()
#   设 结果 为 绑定.libm.sqrt(16.0)

段落 创建预设绑定：
    从 FFI增强 加载 预设绑定()
    。

# =============================================================================
# 辅助工具
# =============================================================================

段落 获取系统信息：
    从 FFI增强 加载 获取系统信息()
    。

段落 列出可用库：
    从 FFI增强 加载 列出可用库()
    。

段落 测试库 接收 库名：文本：
    从 FFI增强 加载 测试库(库名)
    。

# =============================================================================
# 使用示例
# =============================================================================
#
# 示例 1：加载标准 C 库并调用 printf
#   libc = 加载C库("c")
#   调用C函数(libc, "printf", b"Hello, 段言!\\n")
#
# 示例 2：使用预设绑定调用数学函数
#   设 绑定 为 创建预设绑定()
#   设 结果 为 绑定.libm.sqrt(25.0)
#   打印("sqrt(25) = " + 结果)
#
# 示例 3：带类型的精确调用
#   从 ctypes 加载 c_double
#   设 libm 为 加载C库("m")
#   设 结果 为 声明并调用C函数(libm, "pow", [c_double, c_double], c_double, 2.0, 10.0)
#   打印("2^10 = " + 结果)