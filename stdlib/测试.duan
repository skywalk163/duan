# 段言标准库 v3.2 - 测试模块
#
# 用法：
#   从 测试 导入 断言相等, 测试开始, 测试运行。

段落 断言相等 接收 实际, 期望, 消息：
    如果 实际 等于 期望：
        通过数 加上 1
    否则：
        失败数 加上 1
        失败列表.追加(消息)

段落 断言不等 接收 实际, 期望, 消息：
    如果 实际 不等于 期望：
        通过数 加上 1
    否则：
        失败数 加上 1
        失败列表.追加(消息)

段落 断言为真 接收 值, 消息：
    如果 值：
        通过数 加上 1
    否则：
        失败数 加上 1
        失败列表.追加(消息)

段落 断言为假 接收 值, 消息：
    如果 非 值：
        通过数 加上 1
    否则：
        失败数 加上 1
        失败列表.追加(消息)

段落 断言为空 接收 值, 消息：
    如果 长度(值) 等于 0：
        通过数 加上 1
    否则：
        失败数 加上 1
        失败列表.追加(消息)

段落 断言不为空 接收 值, 消息：
    如果 长度(值) 大于 0：
        通过数 加上 1
    否则：
        失败数 加上 1
        失败列表.追加(消息)

段落 断言近似 接收 实际, 期望, 精度, 消息：
    如果 绝对值(实际 减 期望) 小于等于 精度：
        通过数 加上 1
    否则：
        失败数 加上 1
        失败列表.追加(消息)

段落 测试开始 接收 名称：
    设 通过数 等于 0
    设 失败数 等于 0
    设 当前套件名 等于 名称
    设 失败列表 等于 []
    打印 "开始测试：" 加 名称

段落 测试结束：
    设 总数 等于 通过数 加 失败数
    打印 "测试完成：" 加 当前套件名
    打印 "通过：" 加 转串(通过数) 加 "/" 加 转串(总数)
    如果 失败数 大于 0：
        打印 "失败：" 加 转串(失败数) 加 "/" 加 转串(总数)
    否则：
        打印 "全部通过！"

段落 测试运行 接收 测试函数：
    尝试：
        测试函数()
    捕获 异常：
        失败数 加上 1
        失败列表.追加(异常)

导出 断言相等 断言不等。
导出 断言为真 断言为假。
导出 断言为空 断言不为空。
导出 断言近似。
导出 测试开始 测试结束 测试运行。
