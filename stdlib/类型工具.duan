# 段言标准库 - 类型工具模块（纯段言实现）
#
# 用法：
#   从 类型工具 导入 类型名。

# 获取类型名称
段落 类型名。
  参数 数值。
  变量 名称 = 转串(数值)。
  返回 名称。
结束。

# 判断是整数
段落 是整数。
  参数 数值。
  变量 文本 = 转串(数值)。
  如果 长度(文本) == 0：
    返回 假。
  变量 索引 = 0。
  如果 取字符(文本, 0) == "-"：
    如果 长度(文本) == 1：
      返回 假。
    索引 = 1。
  当 索引 < 长度(文本)：
    变量 字符 = 取字符(文本, 索引)。
    如果 字符 < "0" 或 字符 > "9"：
      返回 假。
    索引 = 索引 + 1。
  返回 真。
结束。

# 判断是布尔值
段落 是布尔。
  参数 数值。
  变量 文本 = 转串(数值)。
  如果 文本 == "真" 或 文本 == "假"：
    返回 真。
  返回 假。
结束。

导出 类型名 是整数 是布尔。
