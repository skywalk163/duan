# 段言标准库 - 数学工具模块（纯段言实现）
#
# 用法：
#   从 数学工具 导入 平方, 绝对值, 最大值。

# 平方
段落 平方。
  参数 数值。
  返回 数值 * 数值。
结束。

# 绝对值
段落 绝对值。
  参数 数值。
  如果 数值 < 0：
    返回 0 - 数值。
  返回 数值。
结束。

# 最大值
段落 最大值。
  参数 甲 乙。
  如果 甲 > 乙：
    返回 甲。
  返回 乙。
结束。

# 最小值
段落 最小值。
  参数 甲 乙。
  如果 甲 < 乙：
    返回 甲。
  返回 乙。
结束。

# 判断奇数
段落 是奇数。
  参数 数值。
  如果 数值 模 2 == 1：
    返回 真。
  返回 假。
结束。

# 判断偶数
段落 是偶数。
  参数 数值。
  如果 数值 模 2 == 0：
    返回 真。
  返回 假。
结束。

# 阶乘
段落 阶乘。
  参数 数值。
  如果 数值 <= 1：
    返回 1。
  返回 数值 * 阶乘(数值 - 1)。
结束。

# 判断素数
段落 是素数。
  参数 数值。
  如果 数值 < 2：
    返回 假。
  如果 数值 == 2：
    返回 真。
  如果 是偶数(数值)：
    返回 假。
  变量 甲 = 3。
  当 甲 * 甲 <= 数值：
    如果 数值 模 甲 == 0：
      返回 假。
    甲 = 甲 + 2。
  返回 真。
结束。

# 最大公约数
段落 最大公约数。
  参数 甲 乙。
  当 乙 != 0：
    变量 临时 = 甲 模 乙。
    甲 = 乙。
    乙 = 临时。
  返回 绝对值(甲)。
结束。

# 最小公倍数
段落 最小公倍数。
  参数 甲 乙。
  如果 甲 == 0 或 乙 == 0：
    返回 0。
  变量 乘积 = 甲 * 乙。
  返回 乘积 / 最大公约数(甲, 乙)。
结束。

# 累加求和（1到n）
段落 累加。
  参数 数值。
  变量 总和 = 0。
  变量 计数 = 1。
  当 计数 <= 数值：
    总和 = 总和 + 计数。
    计数 = 计数 + 1。
  返回 总和。
结束。

导出 平方 绝对值 最大值 最小值。
导出 是奇数 是偶数 阶乘 是素数。
导出 最大公约数 最小公倍数 累加。
