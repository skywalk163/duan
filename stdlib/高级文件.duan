段言标准库 - 高级文件模块

封装 shutil 模块，提供高级文件和目录操作功能。

导出：
  复制文件(源路径, 目标路径, 保留元数据) - 复制文件
  复制目录(源路径, 目标路径, 忽略模式) - 递归复制目录
  移动(源路径, 目标路径) - 移动文件或目录
  删除目录(路径, 忽略错误) - 递归删除目录
  删除文件(路径) - 删除文件
  磁盘使用情况(路径) - 获取磁盘使用情况
  磁盘总空间(路径) - 获取磁盘总空间
  磁盘可用空间(路径) - 获取磁盘可用空间
  磁盘已用空间(路径) - 获取磁盘已用空间
  查找命令(命令名) - 查找可执行文件路径
  命令存在(命令名) - 检查命令是否存在
  归档(源路径, 目标路径, 格式) - 创建归档文件
  解压(归档路径, 目标目录, 格式) - 解压归档文件
  支持的归档格式() - 获取支持的归档格式列表
  支持的解压格式() - 获取支持的解压格式列表
  目录大小(路径) - 计算目录总大小
  文件树(路径, 前缀, 显示大小) - 生成目录树状结构字符串
