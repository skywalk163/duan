输出 "你好，段言 v3.1！"。
定义 甲 等于 10。
定义 乙 等于 20。
输出 "甲 加 乙 = " + 转串(甲 加 乙)。
输出 "甲 乘 乙 = " + 转串(甲 乘 乙)。

如果 甲 小于 乙：
    输出 "甲 小于 乙"。
否则：
    输出 "甲 大于等于 乙"。

定义 总和 等于 0。
定义 计数 等于 1。
当 计数 小于等于 5：
    总和 等于 总和 加 计数。
    计数 等于 计数 加 1。
输出 "1到5的和 = " + 转串(总和)。

段落 加法 接收 a, b：
    返回 a 加 b。

输出 "加法(3, 4) = " + 转串(加法(3, 4))。