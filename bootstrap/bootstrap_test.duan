# 段言自举测试 - 验证编译器核心功能

段落 测试数字转换。
  设 测试一 为 数字转整数 五。
  设 测试二 为 数字转整数 三。
  设 测试和 为 测试一 加 测试二。
  打印 测试和。
  返回 测试和。
结束。

段落 测试列表操作。
  设 测试列表 为 列表创建。
  列表追加 测试列表 一。
  列表追加 测试列表 二。
  列表追加 测试列表 三。
  
  设 长度值 为 字符串长度 测试列表。
  打印 长度值。
  
  设 首项 为 列表获取 测试列表 零。
  打印 首项。
  
  返回 长度值。
结束。

段落 测试条件判断 参数 数值。
  如果 数值 大于 五 那么：
    打印 大于五。
    返回 一。
  结束。
  打印 不大于五。
  返回 零。
结束。

段落 测试循环。
  设 计数器 为 零。
  设 总和 为 零。
  
  当 计数器 小于 五 那么：
    设 总和 为 总和 加 计数器。
    设 计数器 为 计数器 加 一。
  结束。
  
  打印 总和。
  返回 总和。
结束。

段落 主。
  打印 =====段言自举测试=====。
  
  打印 测试一数字转换。
  设 结果一 为 测试数字转换。
  
  打印 测试二列表操作。
  设 结果二 为 测试列表操作。
  
  打印 测试三条件判断。
  设 结果三 为 测试条件判断 七。
  
  打印 测试四循环。
  设 结果四 为 测试循环。
  
  打印 =====所有测试完成=====。
  返回 零。
结束。
