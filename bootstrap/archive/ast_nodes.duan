段落 创建数字节点 参数 值。
  设 节点 为 列表创建。
  列表追加 节点 数字。
  列表追加 节点 值。
  返回 节点。
结束。

段落 创建标识符节点 参数 名字。
  设 节点 为 列表创建。
  列表追加 节点 标识符。
  列表追加 节点 名字。
  返回 节点。
结束。

段落 创建二元运算节点 参数 操作符 左侧 右侧。
  设 节点 为 列表创建。
  列表追加 节点 二元运算。
  列表追加 节点 操作符。
  列表追加 节点 左侧。
  列表追加 节点 右侧。
  返回 节点。
结束。

段落 节点类型 参数 节点。
  返回 列表获取 节点 零。
结束。

段落 节点值 参数 节点。
  返回 列表获取 节点 一。
结束。

段落 主。
  设 数字节点 为 创建数字节点 五。
  设 值 为 节点值 数字节点。
  返回 值。
结束。
