# 段言自举测试
# 用段言解释器解释段言代码

从 bootstrap.duan_interpreter 导入 《解释》。

# 测试1: 简单变量定义和打印
定义 测试代码1 等于 "定义 x 等于 42。打印(x)。"。
打印("=== 自举测试1: 变量定义 ===")。
打印("源码: " + 测试代码1)。
打印("执行结果:")。
《解释》(测试代码1)。

# 测试2: 循环
定义 测试代码2 等于 "定义 i 等于 0。当 i 小于 3: 打印(i)。i 等于 i 加 1。结束。"。
打印("")。
打印("=== 自举测试2: 循环 ===")。
打印("源码: " + 测试代码2)。
打印("执行结果:")。
《解释》(测试代码2)。

# 测试3: 条件语句
定义 测试代码3 等于 "定义 x 等于 10。如果 x 大于 5 那么: 打印(\"大于5\")。否则: 打印(\"小于5\")。结束。"。
打印("")。
打印("=== 自举测试3: 条件语句 ===")。
打印("源码: " + 测试代码3)。
打印("执行结果:")。
《解释》(测试代码3)。

打印("")。
打印("=== 自举测试完成 ===")。