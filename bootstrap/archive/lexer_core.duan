# 段言自举词法分析器 - 核心功能测试

段落 创建关键字集合。
  设 关键字字典 为 字典创建。
  字典设置 关键字字典 段落 真。
  字典设置 关键字字典 参数 真。
  字典设置 关键字字典 设 真。
  字典设置 关键字字典 为 真。
  字典设置 关键字字典 返回 真。
  字典设置 关键字字典 结束 真。
  返回 关键字字典。
结束。

段落 是关键字 参数 词 表。
  返回 字典包含键 表 词。
结束。

段落 是中文数字 参数 字符。
  设 数字列表 为 列表创建。
  列表追加 数字列表 零。
  列表追加 数字列表 一。
  列表追加 数字列表 二。
  列表追加 数字列表 三。
  列表追加 数字列表 四。
  列表追加 数字列表 五。
  列表追加 数字列表 六。
  列表追加 数字列表 七。
  列表追加 数字列表 八。
  列表追加 数字列表 九。
  列表追加 数字列表 十。
  返回 列表包含 数字列表 字符。
结束。

段落 中文数字转整数 参数 数字字符。
  如果 数字字符 等于 零 那么：
    返回 零。
  结束。
  如果 数字字符 等于 一 那么：
    返回 一。
  结束。
  如果 数字字符 等于 五 那么：
    返回 五。
  结束。
  返回 零。
结束。

段落 主。
  打印 测试段言词法分析器。
  
  设 关键字表 为 创建关键字集合。
  设 测试词 为 段落。
  设 是关键词 为 是关键字 测试词 关键字表。
  打印 是关键词。
  
  设 测试数字 为 五。
  设 是数字 为 是中文数字 测试数字。
  打印 是数字。
  
  设 数字值 为 中文数字转整数 测试数字。
  打印 数字值。
  
  返回 零。
结束。
