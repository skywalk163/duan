# lexer.duan - 词法分析器
# 用于自举编译器
# 实现三层分词机制（简化版）
#
# 依赖：token.duan（《创建令牌》、《令牌种别集》、《是关键字》、《符号种别》）
#       字符串处理（截取）

导出 《词法分析》。

从《字符串处理》导入《截取》。

# =============================================================================
# 关键字列表（用于最长匹配）
# =============================================================================
《创建关键字列表》段():
  定义列表等于《列表创建》()。
  《列表追加》(列表, "定义")。
  《列表追加》(列表, "等于")。
  《列表追加》(列表, "如果")。
  《列表追加》(列表, "那么")。
  《列表追加》(列表, "否则")。
  《列表追加》(列表, "返回")。
  《列表追加》(列表, "段")。
  《列表追加》(列表, "结束")。
  《列表追加》(列表, "当")。
  《列表追加》(列表, "遍历")。
  《列表追加》(列表, "跳过")。
  《列表追加》(列表, "跳出")。
  《列表追加》(列表, "打印")。
  《列表追加》(列表, "导入")。
  《列表追加》(列表, "导出")。
  《列表追加》(列表, "设")。
  《列表追加》(列表, "为")。
  《列表追加》(列表, "尝试")。
  《列表追加》(列表, "捕获")。
  《列表追加》(列表, "抛出")。
  《列表追加》(列表, "从")。
  《列表追加》(列表, "中的")。
  《列表追加》(列表, "类")。
  《列表追加》(列表, "接口")。
  《列表追加》(列表, "加上")。
  《列表追加》(列表, "减去")。
  《列表追加》(列表, "乘以")。
  《列表追加》(列表, "除以")。
  《列表追加》(列表, "模以")。
  《列表追加》(列表, "幂以")。
  《列表追加》(列表, "真")。
  《列表追加》(列表, "假")。
  《列表追加》(列表, "空")。
  《列表追加》(列表, "且")。
  《列表追加》(列表, "或")。
  《列表追加》(列表, "不")。
  返回 列表。
结束。

# =============================================================================
# 符号种别映射表
# =============================================================================
《创建符号映射》段():
  定义映射等于《字典创建》()。
  《字典设置》(映射, "。", "句号")。
  《字典设置》(映射, "，", "逗号")。
  《字典设置》(映射, "：", "冒号")。
  《字典设置》(映射, "（", "左括号")。
  《字典设置》(映射, "）", "右括号")。
  《字典设置》(映射, "【", "左方括号")。
  《字典设置》(映射, "】", "右方括号")。
  《字典设置》(映射, "《", "左书名号")。
  《字典设置》(映射, "》", "右书名号")。
  《字典设置》(映射, "「", "左引号")。
  《字典设置》(映射, "」", "右引号")。
  《字典设置》(映射, "『", "左单引号")。
  《字典设置》(映射, "』", "右单引号")。
  《字典设置》(映射, ".", "点")。
  《字典设置》(映射, ",", "逗号")。
  《字典设置》(映射, ":", "冒号")。
  《字典设置》(映射, "(", "左括号")。
  《字典设置》(映射, ")", "右括号")。
  《字典设置》(映射, "[", "左方括号")。
  《字典设置》(映射, "]", "右方括号")。
  《字典设置》(映射, "<", "左书名号")。
  《字典设置》(映射, ">", "右书名号")。
  《字典设置》(映射, "\\", "反斜杠")。
  《字典设置》(映射, "=", "等号")。
  《字典设置》(映射, "@", "at")。
  《字典设置》(映射, "+", "加号")。
  返回 映射。
结束。

# =============================================================================
# 字符类型判断
# =============================================================================
《是空白》段(字符):
  返回 字符 等于 " " 或 字符 等于 "\t" 或 字符 等于 "\r"。
结束。

《是数字》段(字符):
  返回 字符 大于等于 "0" 且 字符 小于等于 "9"。
结束。

《是字母》段(字符):
  返回 (字符 大于等于 "a" 且 字符 小于等于 "z") 或 (字符 大于等于 "A" 且 字符 小于等于 "Z") 或 字符 等于 "_"。
结束。

《是标识符字符》段(字符, 符号字典):
  如果 《是空白》(字符) 那么 返回 假。
  如果 字符 等于 "\n" 那么 返回 假。
  如果 字符 等于 "\"" 或 字符 等于 "'" 那么 返回 假。
  如果 《字典包含键》(符号字典, 字符) 那么 返回 假。
  返回 真。
结束。

# =============================================================================
# 最长关键字匹配
# =============================================================================
《匹配关键字》段(文本, 关键字列表):
  定义最佳关键字等于空。
  定义索引等于0。
  定义列表长度等于《列表长度》(关键字列表)。
  当 索引 小于 列表长度:
    定义关键字等于《列表获取》(关键字列表, 索引)。
    定义关键字长度等于《字符串长度》(关键字)。
    定义文本长度等于《字符串长度》(文本)。
    如果 关键字长度 大于 0 且 关键字长度 小于等于 文本长度:
      如果 《截取》(文本, 0, 关键字长度) 等于 关键字:
        如果 最佳关键字 等于 空 或 关键字长度 大于 《字符串长度》(最佳关键字):
          设 最佳关键字 为 关键字。
    设 索引 为 索引 加 1。
  返回 最佳关键字。
结束。

# =============================================================================
# 收集标识符（连续的非符号、非空白字符）
# =============================================================================
《收集标识符文本》段(源码, 位置, 符号字典):
  定义结果等于""。
  定义源码长度等于《字符串长度》(源码)。
  当 位置 小于 源码长度:
    定义字符等于《字符串获取》(源码, 位置)。
    如果 《是标识符字符》(字符, 符号字典):
      设 结果 为 结果 加 字符。
      设 位置 为 位置 加 1。
    否则:
      跳出。
  返回 结果。
结束。

# =============================================================================
# 标识符分词：将文本按关键字拆分为令牌列表
# 例如 "定义甲" → [关键字"定义", 标识符"甲"]
# =============================================================================
《标识符分词》段(文本, 关键字列表, 行, 列):
  定义结果等于《列表创建》()。
  定义剩余文本等于文本。
  定义当前列等于列。
  
  当 《字符串长度》(剩余文本) 大于 0:
    定义找到关键字等于《匹配关键字》(剩余文本, 关键字列表)。
    如果 找到关键字 不等于 空:
      定义找到长度等于《字符串长度》(找到关键字)。
      《列表追加》(结果, 《创建令牌》("关键字", 找到关键字, 行, 当前列))。
      设 当前列 为 当前列 加 找到长度。
      设 剩余文本 为 《截取》(剩余文本, 找到长度, 《字符串长度》(剩余文本))。
    否则:
      # 未匹配到关键字，整个剩余作为标识符
      《列表追加》(结果, 《创建令牌》("标识符", 剩余文本, 行, 当前列))。
      设 剩余文本 为 ""。
  返回 结果。
结束。

# =============================================================================
# 跳过注释到行尾
# =============================================================================
《跳到行尾》段(源码, 位置):
  定义源码长度等于《字符串长度》(源码)。
  当 位置 小于 源码长度:
    定义字符等于《字符串获取》(源码, 位置)。
    如果 字符 等于 "\n":
      返回 位置。
    设 位置 为 位置 加 1。
  返回 位置。
结束。

# 主词法分析函数（返回令牌列表）
《词法分析》段(源码):
  定义令牌列表等于《列表创建》()。
  定义源码长度等于《字符串长度》(源码)。
  定义位置等于0。
  定义行等于1。
  定义列等于1。
  定义缩进栈等于《列表创建》()。
  《列表追加》(缩进栈, 0)。
  定义关键字列表等于《创建关键字列表》()。
  定义符号映射等于《创建符号映射》()。
  
  当 位置 小于 源码长度:
    定义字符等于《字符串获取》(源码, 位置)。
    
    # ── 换行处理 ──
    如果 字符 等于 "\n":
      《列表追加》(令牌列表, 《创建令牌》("换行", "\n", 行, 列))。
      设 行 为 行 加 1。
      设 列 为 1。
      设 位置 为 位置 加 1。
      
      # 计算缩进
      定义缩进等于0。
      当 位置 小于 源码长度:
        定义下一个等于《字符串获取》(源码, 位置)。
        如果 下一个 等于 " ":
          设 缩进 为 缩进 加 1。
          设 位置 为 位置 加 1。
        否则 如果 下一个 等于 "\t":
          设 缩进 为 缩进 加 4。
          设 位置 为 位置 加 1。
        否则:
          跳出。
      
      设 列 为 缩进 加 1。
      
      # 处理缩进变化
      定义栈顶等于《列表获取》(缩进栈, 《列表长度》(缩进栈) 减 1)。
      如果 缩进 大于 栈顶:
        《列表追加》(缩进栈, 缩进)。
        《列表追加》(令牌列表, 《创建令牌》("缩进", 缩进, 行, 1))。
      否则 如果 缩进 小于 栈顶:
        当 《列表获取》(缩进栈, 《列表长度》(缩进栈) 减 1) 大于 缩进:
          《列表弹出》(缩进栈)。
          《列表追加》(令牌列表, 《创建令牌》("反缩进", 《列表获取》(缩进栈, 《列表长度》(缩进栈) 减 1), 行, 1))。
      跳过。
    
    # ── 空白跳过 ──
    如果 《是空白》(字符):
      设 列 为 列 加 1。
      设 位置 为 位置 加 1。
      跳过。
    
    # ── 注释 # ──
    如果 字符 等于 "#":
      设 位置 为 《跳到行尾》(源码, 位置)。
      跳过。
    
    # ── 注释 // ──
    如果 字符 等于 "/" 且 位置 加 1 小于 源码长度:
      定义下一个字符等于《字符串获取》(源码, 位置 加 1)。
      如果 下一个字符 等于 "/":
        设 位置 为 《跳到行尾》(源码, 位置)。
        跳过。
    
    # ── 注释 注 ──
    如果 字符 等于 "注":
      定义位置备份等于位置。
      设 位置 为 《跳到行尾》(源码, 位置)。
      定义消耗等于位置减去位置备份。
      如果 消耗 等于 1:
        # 只是"注"这个字，作为标识符处理（不跳过）
        # 回退
        设 位置 为 位置备份。
      否则:
        跳过。
    
    # ── 管道操作符 -> ──
    如果 字符 等于 "-":
      如果 位置 加 1 小于 源码长度 且 《字符串获取》(源码, 位置 加 1) 等于 ">":
        《列表追加》(令牌列表, 《创建令牌》("箭头", "->", 行, 列))。
        设 列 为 列 加 2。
        设 位置 为 位置 加 2。
        跳过。
    
    # ── 书名号《》处理 ──
    如果 字符 等于 "《":
      《列表追加》(令牌列表, 《创建令牌》("左书名号", "《", 行, 列))。
      设 列 为 列 加 1。
      设 位置 为 位置 加 1。
      
      # 收集段落名（连续的标识符字符直到 》）
      定义名称等于""。
      当 位置 小于 源码长度:
        定义名称字符等于《字符串获取》(源码, 位置)。
        如果 名称字符 等于 "》":
          跳出。
        设 名称 为 名称 加 名称字符。
        设 位置 为 位置 加 1。
        设 列 为 列 加 1。
      
      《列表追加》(令牌列表, 《创建令牌》("标识符", 名称, 行, 列))。
      
      # 消耗 》  
      如果 位置 小于 源码长度:
        设 列 为 列 加 1。
        设 位置 为 位置 加 1。
        《列表追加》(令牌列表, 《创建令牌》("右书名号", "》", 行, 列))。
      跳过。
    
    # ── 符号处理 ──
    如果 《字典包含键》(符号映射, 字符):
      定义符号种别等于《字典获取》(符号映射, 字符)。
      《列表追加》(令牌列表, 《创建令牌》(符号种别, 字符, 行, 列))。
      设 列 为 列 加 1。
      设 位置 为 位置 加 1。
      跳过。
    
    # ── 字符串处理 ──
    如果 字符 等于 "\"" 或 字符 等于 "'":
      定义字符串结果等于""。
      定义起始位置等于位置。
      设 位置 为 位置 加 1。
      当 位置 小于 源码长度:
        定义字符串字符等于《字符串获取》(源码, 位置)。
        如果 字符串字符 等于 字符:
          设 位置 为 位置 加 1。
          跳出。
        否则 如果 字符串字符 等于 "\\":
          设 位置 为 位置 加 1。
          如果 位置 小于 源码长度:
            定义转义字符等于《字符串获取》(源码, 位置)。
            如果 转义字符 等于 "n":
              设 字符串结果 为 字符串结果 加 "\n"。
            否则 如果 转义字符 等于 "t":
              设 字符串结果 为 字符串结果 加 "\t"。
            否则:
              设 字符串结果 为 字符串结果 加 转义字符。
            设 位置 为 位置 加 1。
        否则:
          设 字符串结果 为 字符串结果 加 字符串字符。
          设 位置 为 位置 加 1。
      《列表追加》(令牌列表, 《创建令牌》("字符串", 字符串结果, 行, 列))。
      设 列 为 列 加 (位置 减 起始位置)。
      跳过。
    
    # ── 数字处理 ──
    如果 《是数字》(字符) 或 (字符 等于 "-" 且 位置 加 1 小于 源码长度 且 《是数字》(《字符串获取》(源码, 位置 加 1))):
      定义数字开始等于位置。
      定义是负数等于假。
      如果 字符 等于 "-":
        设 是负数 为 真。
        设 位置 为 位置 加 1。
      定义数字字符串等于""。
      如果 是负数:
        设 数字字符串 为 "-"。
      当 位置 小于 源码长度:
        定义数字字符等于《字符串获取》(源码, 位置)。
        如果 《是数字》(数字字符):
          设 数字字符串 为 数字字符串 加 数字字符。
          设 位置 为 位置 加 1。
        否则:
          跳出。
      # 检查小数部分
      如果 位置 小于 源码长度 且 《字符串获取》(源码, 位置) 等于 ".":
        定义下一个下一个等于位置加1。
        如果 下一个下一个 小于 源码长度 且 《是数字》(《字符串获取》(源码, 下一个下一个)):
          设 数字字符串 为 数字字符串 加 "."。
          设 位置 为 位置 加 1。
          当 位置 小于 源码长度:
            定义数字字符等于《字符串获取》(源码, 位置)。
            如果 《是数字》(数字字符):
              设 数字字符串 为 数字字符串 加 数字字符。
              设 位置 为 位置 加 1。
            否则:
              跳出。
      《列表追加》(令牌列表, 《创建令牌》("数字", 数字字符串, 行, 列))。
      设 列 为 列 加 (位置 减 数字开始)。
      跳过。
    
    # ── 汉字/标识符/关键字处理 ──
    如果 字符 大于 "\x7F" 或 《是字母》(字符):
      定义标识符文本等于《收集标识符文本》(源码, 位置, 符号映射)。
      定义标识符长度等于《字符串长度》(标识符文本)。
      
      如果 标识符长度 大于 0:
        定义分词结果等于《标识符分词》(标识符文本, 关键字列表, 行, 列)。
        定义分词索引等于0。
        当 分词索引 小于 《列表长度》(分词结果):
          《列表追加》(令牌列表, 《列表获取》(分词结果, 分词索引))。
          设 分词索引 为 分词索引 加 1。
        设 位置 为 位置 加 标识符长度。
        设 列 为 列 加 标识符长度。
        跳过。
    
    # ── 未知字符 ──
    定义错误消息等于"未知字符"。
    《列表追加》(令牌列表, 《创建令牌》("错误", 错误消息, 行, 列))。
    设 位置 为 位置 加 1。
    设 列 为 列 加 1。
  结束。
  
  # 文件结束，处理剩余的反缩进
  当 《列表长度》(缩进栈) 大于 1:
    《列表弹出》(缩进栈)。
    《列表追加》(令牌列表, 《创建令牌》("反缩进", 《列表获取》(缩进栈, 《列表长度》(缩进栈) 减 1), 行, 列))。
  
  # 添加 EOF 标记
  《列表追加》(令牌列表, 《创建令牌》("结束", 空, 行, 列))。
  
  返回 令牌列表。
结束。