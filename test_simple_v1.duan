# 段言语言特性测试 - 简化版

段落 测试一。
  设 数值 为 五。
  打印 数值。
  返回 数值。
结束。

段落 测试二 参数 甲 乙。
  设 结果值 为 甲 加 乙。
  返回 结果值。
结束。

段落 测试三。
  设 列表值 为 列表创建。
  列表追加 列表值 一。
  列表追加 列表值 二。
  
  设 长度值 为 字符串长度 列表值。
  打印 长度值。
  返回 长度值。
结束。

段落 主。
  打印 测试段言语言特性。
  
  设 结果一 为 测试一。
  打印 结果一。
  
  设 结果二 为 测试二 三 五。
  打印 结果二。
  
  设 结果三 为 测试三。
  打印 结果三。
  
  返回 零。
结束。
