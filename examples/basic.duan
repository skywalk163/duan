# 段言编程语言 - 示例项目
# 示例1: 基础语法演示

定义甲等于123。
定义乙等于三加五。
打印("变量声明：")。
打印(甲)。
打印(乙)。

定义结果等于甲加乙乘二。
打印("算术运算：")。
打印(结果)。

打印("条件语句：")。
如果甲大于乙那么打印"甲大于乙"。
否则打印"甲小于等于乙"。

《加法》段(甲, 乙)：
  返回甲加乙。
结束。

定义和等于《加法》(三, 五)。
打印("函数调用：")。
打印(和)。

《阶乘》段(数)：
  如果数小于等于一那么返回一。
  返回数乘《阶乘》(数减一)。
结束。

定义阶乘结果等于《阶乘》(五)。
打印("递归函数（阶乘）：")。
打印(阶乘结果)。

打印("循环示例：")。
定义计数等于一。
当 计数 小于等于 五：
  打印(计数)。
  定义计数等于计数加一。
结束。

打印("程序执行完成！")。