# 段言 - 数据结构算法库
# 包含链表、栈、队列、二叉树等数据结构实现
# 用法: duan run examples/algorithms/data_structures.duan

# ===== 1. 链表 (Linked List) =====
类 链表节点：
  属性 值。
  属性 下一个。

  构造 接收 值参数：
    己值 为 值参数。
    己下一个 为 空。

类 链表：
  属性 头。
  属性 长度。

  构造：
    己头 为 空。
    己长度 为 0。

  段落 追加 接收 值：
    设 新节点 为 新建 链表节点(值)。
    如果 己头 是 空：
      己头 为 新节点。
    否则：
      设 当前 为 己头。
      当 当前.下一个 不是 空：
        当前 为 当前.下一个。
      当前.下一个 为 新节点。
    己长度 为 己长度 + 1。

  段落 插入 接收 索引, 值：
    如果 索引 小于 0 或 索引 大于 己长度：
      返回 假。

    设 新节点 为 新建 链表节点(值)。
    如果 索引 等于 0：
      新节点.下一个 为 己头。
      己头 为 新节点。
    否则：
      设 当前 为 己头。
      设 i 为 0。
      当 i 小于 索引 - 1：
        当前 为 当前.下一个。
        i 为 i + 1。
      新节点.下一个 为 当前.下一个。
      当前.下一个 为 新节点。
    己长度 为 己长度 + 1。
    返回 真。

  段落 删除 接收 值：
    如果 己头 是 空：
      返回 假。
    如果 己头.值 等于 值：
      己头 为 己头.下一个。
      己长度 为 己长度 - 1。
      返回 真。

    设 当前 为 己头。
    当 当前.下一个 不是 空：
      如果 当前.下一个.值 等于 值：
        当前.下一个 为 当前.下一个.下一个。
        己长度 为 己长度 - 1。
        返回 真。
      当前 为 当前.下一个。
    返回 假。

  段落 查找 接收 值：
    设 当前 为 己头。
    设 索引 为 0。
    当 当前 不是 空：
      如果 当前.值 等于 值：
        返回 索引。
      当前 为 当前.下一个。
      索引 为 索引 + 1。
    返回 -1。

  段落 获取 接收 索引：
    如果 索引 小于 0 或 索引 大于等于 己长度：
      返回 空。
    设 当前 为 己头。
    设 i 为 0。
    当 i 小于 索引：
      当前 为 当前.下一个。
      i 为 i + 1。
    返回 当前.值。

  段落 转列表：
    设 结果 为 []。
    设 当前 为 己头。
    当 当前 不是 空：
      列表追加(结果, 当前.值)。
      当前 为 当前.下一个。
    返回 结果。

  段落 打印：
    设 当前 为 己头。
    设 字符串 为 ""。
    当 当前 不是 空：
      字符串 为 字符串 + 转字符串(当前.值)。
      如果 当前.下一个 不是 空：
        字符串 为 字符串 + " -> "。
      当前 为 当前.下一个。
    打印(字符串)。

# ===== 2. 栈 (Stack) =====
类 栈：
  属性 元素。

  构造：
    己元素 为 []。

  段落 压入 接收 值：
    列表追加(己元素, 值)。

  段落 弹出：
    如果 列表长度(己元素) 等于 0：
      返回 空。
    返回 列表弹出(己元素)。

  段落 查看栈顶：
    如果 列表长度(己元素) 等于 0：
      返回 空。
    返回 己元素[列表长度(己元素) - 1]。

  段落 是否为空：
    返回 列表长度(己元素) 等于 0。

  段落 大小：
    返回 列表长度(己元素)。

  段落 打印：
    设 结果 为 "栈[底部]: "。
    设 i 为 0。
    当 i 小于 列表长度(己元素)：
      结果 为 结果 + 转字符串(己元素[i]) + " "。
      i 为 i + 1。
    结果 为 结果 + "[顶部]"。
    打印(结果)。

# ===== 3. 队列 (Queue) =====
类 队列：
  属性 元素。

  构造：
    己元素 为 []。

  段落 入队 接收 值：
    列表追加(己元素, 值)。

  段落 出队：
    如果 列表长度(己元素) 等于 0：
      返回 空。
    设 值 为 己元素[0]。
    设 新列表 为 []。
    设 i 为 1。
    当 i 小于 列表长度(己元素)：
      列表追加(新列表, 己元素[i])。
      i 为 i + 1。
    己元素 为 新列表。
    返回 值。

  段落 查看队首：
    如果 列表长度(己元素) 等于 0：
      返回 空。
    返回 己元素[0]。

  段落 是否为空：
    返回 列表长度(己元素) 等于 0。

  段落 大小：
    返回 列表长度(己元素)。

  段落 打印：
    设 结果 为 "队列[队首]: "。
    遍历 值 于 己元素：
      结果 为 结果 + 转字符串(值) + " "。
    结果 为 结果 + "[队尾]"。
    打印(结果)。

# ===== 4. 二叉树节点 (Binary Tree) =====
类 二叉树节点：
  属性 值。
  属性 左孩子。
  属性 右孩子。

  构造 接收 值参数：
    己值 为 值参数。
    己左孩子 为 空。
    己右孩子 为 空。

# ===== 5. 二叉搜索树 (Binary Search Tree) =====
类 二叉搜索树：
  属性 根。

  构造：
    己根 为 空。

  段落 插入 接收 值：
    设 新节点 为 新建 二叉树节点(值)。
    如果 己根 是 空：
      己根 为 新节点。
      返回。

    设 当前 为 己根。
    当 真：
      如果 值 小于 当前.值：
        如果 当前.左孩子 是 空：
          当前.左孩子 为 新节点。
          返回。
        否则：
          当前 为 当前.左孩子。
      否则：
        如果 当前.右孩子 是 空：
          当前.右孩子 为 新节点。
          返回。
        否则：
          当前 为 当前.右孩子。

  段落 查找 接收 值：
    设 当前 为 己根。
    当 当前 不是 空：
      如果 当前.值 等于 值：
        返回 真。
      否则若 值 小于 当前.值：
        当前 为 当前.左孩子。
      否则：
        当前 为 当前.右孩子。
    返回 假。

  段落 前序遍历 接收 节点：
    如果 节点 是 空：
      返回。
    打印(转字符串(节点.值))。
    己前序遍历(节点.左孩子)。
    己前序遍历(节点.右孩子)。

  段落 中序遍历 接收 节点：
    如果 节点 是 空：
      返回。
    己中序遍历(节点.左孩子)。
    打印(转字符串(节点.值))。
    己中序遍历(节点.右孩子)。

  段落 后序遍历 接收 节点：
    如果 节点 是 空：
      返回。
    己后序遍历(节点.左孩子)。
    己后序遍历(节点.右孩子)。
    打印(转字符串(节点.值))。

  段落 获取最小值：
    如果 己根 是 空：
      返回 空。
    设 当前 为 己根。
    当 当前.左孩子 不是 空：
      当前 为 当前.左孩子。
    返回 当前.值。

  段落 获取最大值：
    如果 己根 是 空：
      返回 空。
    设 当前 为 己根。
    当 当前.右孩子 不是 空：
      当前 为 当前.右孩子。
    返回 当前.值。

  段落 计算高度 接收 节点：
    如果 节点 是 空：
      返回 0。
    设 左高 为 己计算高度(节点.左孩子)。
    设 右高 为 己计算高度(节点.右孩子)。
    返回 1 + (左高 如果 左高 大于 右高 否则 右高)。

  段落 打印中序：
    打印("中序遍历: ")。
    己中序遍历(己根)。
    打印("")。

# ===== 6. 双端队列 (Deque) =====
类 双端队列：
  属性 元素。

  构造：
    己元素 为 []。

  段落 前端添加 接收 值：
    设 新列表 为 [值]。
    遍历 项 于 己元素：
      列表追加(新列表, 项)。
    己元素 为 新列表。

  段落 后端添加 接收 值：
    列表追加(己元素, 值)。

  段落 前端移除：
    如果 列表长度(己元素) 等于 0：
      返回 空。
    设 值 为 己元素[0]。
    设 新列表 为 []。
    设 i 为 1。
    当 i 小于 列表长度(己元素)：
      列表追加(新列表, 己元素[i])。
      i 为 i + 1。
    己元素 为 新列表。
    返回 值。

  段落 后端移除：
    如果 列表长度(己元素) 等于 0：
      返回 空。
    返回 列表弹出(己元素)。

  段落 是否为空：
    返回 列表长度(己元素) 等于 0。

  段落 大小：
    返回 列表长度(己元素)。

# ===== 7. 优先队列 (使用数组实现) =====
类 优先队列：
  属性 元素。

  构造：
    己元素 为 []。

  段落 插入 接收 值, 优先级：
    设 项 为 {"value": 值, "priority": 优先级}。
    设 添加 为 假。
    设 新列表 为 []。
    遍历 现有 于 己元素：
      如果 添加 等于 假 且 现有["priority"] 大于 优先级：
        列表追加(新列表, 项)。
        添加 为 真。
      列表追加(新列表, 现有)。
    如果 添加 等于 假：
      列表追加(新列表, 项)。
    己元素 为 新列表。

  段落 取出：
    如果 列表长度(己元素) 等于 0：
      返回 空。
    设 项 为 己元素[0]。
    设 新列表 为 []。
    设 i 为 1。
    当 i 小于 列表长度(己元素)：
      列表追加(新列表, 己元素[i])。
      i 为 i + 1。
    己元素 为 新列表。
    返回 项["value"]。

  段落 是否为空：
    返回 列表长度(己元素) 等于 0。

# ===== 测试所有数据结构 =====
段落 测试链表：
  打印("1. 链表测试:")。
  设 列表 为 新建 链表()。
  列表.追加(10)。
  列表.追加(20)。
  列表.追加(30)。
  列表.插入(1, 15)。
  打印("   链表内容: ")。
  列表.打印()。
  打印("   长度: " + 转字符串(列表.长度))。
  打印("   查找 20: 索引 " + 转字符串(列表.查找(20)))。
  列表.删除(20)。
  打印("   删除 20 后: ")。
  列表.打印()。
  打印("   转列表: " + 转字符串(列表.转列表()))。

段落 测试栈：
  打印("\n2. 栈测试:")。
  设 栈 为 新建 栈()。
  栈.压入(1)。
  栈.压入(2)。
  栈.压入(3)。
  栈.打印()。
  打印("   弹出: " + 转字符串(栈.弹出()))。
  打印("   栈顶: " + 转字符串(栈.查看栈顶()))。
  栈.打印()。
  打印("   大小: " + 转字符串(栈.大小()))。

段落 测试队列：
  打印("\n3. 队列测试:")。
  设 队列 为 新建 队列()。
  队列.入队("A")。
  队列.入队("B")。
  队列.入队("C")。
  队列.打印()。
  打印("   出队: " + 转字符串(队列.出队()))。
  打印("   队首: " + 转字符串(队列.查看队首()))。
  队列.打印()。

段落 测试二叉搜索树：
  打印("\n4. 二叉搜索树测试:")。
  设 bst 为 新建 二叉搜索树()。
  bst.插入(50)。
  bst.插入(30)。
  bst.插入(70)。
  bst.插入(20)。
  bst.插入(40)。
  bst.插入(60)。
  bst.插入(80)。
  bst.打印中序()。
  打印("   查找 40: " + 转字符串(bst.查找(40)))。
  打印("   查找 100: " + 转字符串(bst.查找(100)))。
  打印("   最小值: " + 转字符串(bst.获取最小值()))。
  打印("   最大值: " + 转字符串(bst.获取最大值()))。
  打印("   树高度: " + 转字符串(bst.计算高度(bst.根)))。

段落 测试双端队列：
  打印("\n5. 双端队列测试:")。
  设 双端 为 新建 双端队列()。
  双端.后端添加(2)。
  双端.后端添加(3)。
  双端.前端添加(1)。
  打印("   前端移除: " + 转字符串(双端.前端移除()))。
  打印("   后端移除: " + 转字符串(双端.后端移除()))。
  打印("   是否为空: " + 转字符串(双端.是否为空()))。
  双端.后端添加(4)。
  打印("   大小: " + 转字符串(双端.大小()))。

段落 测试优先队列：
  打印("\n6. 优先队列测试:")。
  设 pq 为 新建 优先队列()。
  pq.插入("任务A", 3)。
  pq.插入("任务B", 1)。
  pq.插入("任务C", 2)。
  打印("   取出: " + pq.取出())。
  打印("   取出: " + pq.取出())。
  打印("   取出: " + pq.取出())。
  打印("   是否为空: " + 转字符串(pq.是否为空()))。

段落 测试所有：
  打印("===== 数据结构算法测试 =====")。
  打印("包含 7 种数据结构实现。\n")。
  测试链表()。
  测试栈()。
  测试队列()。
  测试二叉搜索树()。
  测试双端队列()。
  测试优先队列()。
  打印("\n===== 全部测试完成 =====")。

测试所有()。