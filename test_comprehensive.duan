# 综合测试 - ANTLR解析器功能验证

# 1. 变量声明
设 甲 为 十。
设 乙 为 二十。
设 丙 为 "你好，世界！"。

# 2. 段落定义
段落 平方 接收 数值：
  返回 数值 乘 数值。
结束。

段落 求和 接收 甲, 乙：
  返回 甲 加 乙。
结束。

段落 最大值 接收 甲, 乙：
  如果 甲 大于 乙：
    返回 甲。
  结束。
  返回 乙。
结束。

# 3. 函数调用
设 结果1 为 平方(五)。
设 结果2 为 求和(十, 二十)。
设 结果3 为 最大值(甲, 乙)。

# 4. 打印输出
打印 结果1。
打印 结果2。
打印 结果3。
打印 丙。

# 5. 控制流
设 计数 为 零。
当 计数 小于 五：
  打印 计数。
  设 计数 为 计数 加 一。
结束。

# 6. 列表操作
设 列表 为 [1, 2, 3, 4, 5]。
遍历 项目 于 列表：
  打印 项目。
结束。
